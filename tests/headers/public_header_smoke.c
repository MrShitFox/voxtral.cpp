#include <voxtral.h>
#include <voxtral-stream.h>

int main(void) {
    voxtral_model * model = 0;
    voxtral_context * context = 0;
    voxtral_stream * stream = 0;
    voxtral_stream_params params = {0};
    (void) model;
    (void) context;
    (void) stream;
    (void) params;
    return VOXTRAL_STATUS_OK;
}
